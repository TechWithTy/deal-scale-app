#Add Uploadthing secret and appId to setup fileupload https://uploadthing.com/

UPLOADTHING_SECRET=
UPLOADTHING_APP_ID=

# Image stack + canonical URLs (used by Cloudflare image loader fallback)
NEXT_IMAGE_STACK=next # set to "cloudflare" in Pages/Workers
NEXT_PUBLIC_APP_URL=http://localhost:3000
# NEXT_IMAGE_DEFAULT_QUALITY=75

# Next auth https://next-auth.js.org/configuration/options
# Run this command to generate a a new NEXTAUTH_SECRET
# $ openssl rand -base64 32

NEXTAUTH_URL = http://localhost:3000
NEXTAUTH_SECRET=

# Go to github and setup the oauth configuration
# https://next-auth.js.org/providers/github#configuration
# https://github.com/settings/developers

GITHUB_ID = 
GITHUB_SECRET = 


## Google Maps (for lead search autocomplete and maps)
# Enable "Maps JavaScript API" and "Places API" on your Google Cloud project
# Add the dev referrers to your API key restrictions (e.g. http://localhost:3000, http://127.0.0.1:3000)
# One of the two keys below is sufficient — choose your convention
NEXT_PUBLIC_GMAPS_KEY=
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
# Optional vector map style ID
NEXT_PUBLIC_GMAPS_MAP_ID=

## Twilio (optional, for Number Pooling connected numbers fetch)
# Used by /api/twilio/numbers to list your connected phone numbers
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=

# Action Bar AI Suggest Proxy
# Comma-separated list of allowed origins for CORS (use * to allow any; not recommended for production)
ACTION_BAR_CORS_ORIGINS=http://localhost:3000
# One or more API keys accepted by the /api/ai/command-suggest endpoint (comma-separated)
# Clients can send via Authorization: Bearer <key>, X-Deal-Action-Key: <key>, or ?key=<key>
ACTION_BAR_API_KEYS=dev-key-123

# --- Analytics: PostHog (optional) ---
# Master toggle
NEXT_PUBLIC_ENABLE_POSTHOG=false
# Keys/host (find in PostHog project settings)
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
# Optional
NEXT_PUBLIC_POSTHOG_CAPTURE_PAGEVIEW=true
NEXT_PUBLIC_POSTHOG_DEBUG=false
NEXT_PUBLIC_POSTHOG_SESSION_RECORDING=false

# --- Analytics: Microsoft Clarity (optional) ---
# Master toggle and Project ID
NEXT_PUBLIC_ENABLE_CLARITY=false
NEXT_PUBLIC_CLARITY_ID=

# --- Admin impersonation ---
# Force the front-end to use the mock impersonation API instead of calling the backend
NEXT_PUBLIC_IMPERSONATION_USE_MOCK=true

# --- Focus Music Testing ---
# Force the floating focus music widget to appear with mock data
NEXT_PUBLIC_MUSIC_WIDGET_DEBUG=false
# Optional: override the mock playlist URI used when debug mode is enabled
NEXT_PUBLIC_MUSIC_WIDGET_DEBUG_PLAYLIST=spotify:playlist:37i9dQZF1DX8Uebhn9wzrS

# --- Spotify Integration ---
# Required for Spotify OAuth and SDK integration
# Register your app at https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REDIRECT_URI=http://localhost:3000/api/spotify/callback
SPOTIFY_SCOPES=user-read-playback-state user-modify-playback-state streaming user-read-currently-playing user-read-private user-read-email playlist-read-private playlist-read-collaborative
# Optional: Service account refresh token (for background playback without user OAuth)
SPOTIFY_REFRESH_TOKEN=
# Required for client-side SDK initialization (@spotify/web-api-ts-sdk)
NEXT_PUBLIC_SPOTIFY_CLIENT_ID=

# --- PWA Testing (Local Development) ---
# By default, PWA features (service workers, offline detection) only work in production builds
# To test locally: pnpm build && pnpm start (or use pnpm test:pwa:local)
# To disable PWA completely (useful for debugging)
# NEXT_DISABLE_PWA=1

